// فارسی: کتابخانهٔ کوچکی برای تعریف تابع جمع است و توسط مثال include_once بارگذاری می‌شود.
// English: Defines a small addition library loaded by the include_once example.
func add(number a, number b) -> number {
    return a + b;
}
